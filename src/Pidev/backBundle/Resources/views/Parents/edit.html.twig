{% extends "PidevbackBundle::layout.html.twig" %} 
{% block body %}
    <h1>Parents edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    
        <a class="btn btn-default"  role="btn"  href="{{ path('ajoutparent') }}">
            Back to the list
        </a>
    
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

